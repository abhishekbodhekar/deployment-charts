datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      url: http://prod-prometheus-server.default.svc.cluster.local
      access: proxy
      isDefault: true
    
service:
  type: LoadBalancer
  port: 3000
